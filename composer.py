"""Compatibility wrapper for the message composer.

The implementation lives in messaging/composer.py so related messaging logic can
be grouped together without breaking older imports or deployment entrypoints.
"""

from messaging.composer import Composer

